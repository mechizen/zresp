# samples

`/content/sample.{ext}` のうち、生成が難しいバイナリ形式は**ここに置いた実ファイル**を配信します。

対象拡張子: `mp4` / `webm` / `ttf` / `woff` / `woff2`

ファイル名は `sample.{ext}`(例: `sample.mp4`, `sample.woff2`)にしてください。
未配置の拡張子にアクセスすると `404`(配置を促すヒント付き)を返します。

> 画像(png/jpg/gif/webp)・PDF・テキスト系は動的生成するため、ここに置く必要はありません。
